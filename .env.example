# https://developer.spotify.com/dashboard — create an app, copy Client ID
VITE_SPOTIFY_CLIENT_ID=

# Optional; defaults to current origin (must match an app Redirect URI exactly)
# VITE_SPOTIFY_REDIRECT_URI=http://127.0.0.1:5173/
